# 内规数据目录

此目录存放待解析的内部规章制度文档。

## 支持格式

- `.docx` (Word 文档)
- `.doc` (旧版 Word 文档)

## 数据格式要求

文档应按章节/条款划分，每条条款应包含条号（如"第一条"、"第十五条"等），以便解析器正确切分。

## 使用方式

将内部制度 DOCX 文件放入此目录后，运行流水线：

```bash
python run_concurrent.py --candidates 20000
```
